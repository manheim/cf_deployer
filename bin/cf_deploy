#!/usr/bin/env ruby

require_relative '../lib/cf_deployer'

begin
 CfDeployer::CLI.start(ARGV)
rescue CfDeployer::ApplicationError => e
  if CfDeployer::Log.level == 'debug'
    puts "Error: #{e.message}"
  else
    puts $!, $@
  end
  exit(1)
end
